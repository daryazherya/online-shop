import Box from '@mui/material/Box';
import { LinearProgress } from '@mui/material';

export function ProgressLine() {
  return (
    <Box sx={{ width: '100%' }}>
      <LinearProgress />
    </Box>
  );
}